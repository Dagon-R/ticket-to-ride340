package Services;

public class StartGameService implements Service {

    @Override
    public Object doService(Object... obj) {
        String gameID = (String) obj[0];

        //call StartGameService
        //returns game obj

        //create activeGame (and add to list of activeGames)
        //return activeGame
        return null;
    }
}
